name: Publish npm packages

on:
  workflow_dispatch:
  push:
    tags:
      - "packages-v*"

permissions:
  contents: read

jobs:
  build:
    name: Build packages
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build packages
        run: npm run build:packages

      - name: Check package contents
        run: |
          npm pack --dry-run --workspace packages/sdk
          npm pack --dry-run --workspace packages/react
          npm pack --dry-run --workspace packages/vite-plugin
          npm pack --dry-run --workspace packages/next
          npm pack --dry-run --workspace packages/nest
          npm pack --dry-run --workspace packages/express
          npm pack --dry-run --workspace packages/angular

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: package-dist
          path: |
            packages/*/dist/
          retention-days: 1

  publish:
    name: Publish packages to npm
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: "https://registry.npmjs.org"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: package-dist
          path: packages

      - name: Verify trusted publishing toolchain
        run: |
          node --version
          npm --version

      - name: Publish packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm publish --provenance --access public --workspace packages/sdk
          npm publish --provenance --access public --workspace packages/react
          npm publish --provenance --access public --workspace packages/vite-plugin
          npm publish --provenance --access public --workspace packages/next
          npm publish --provenance --access public --workspace packages/nest
          npm publish --provenance --access public --workspace packages/express
          npm publish --provenance --access public --workspace packages/angular
